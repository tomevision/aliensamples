tosca_definitions_version: alien_dsl_1_4_0

metadata:
  template_name: file-types
  template_version: 1.0.0-SNAPSHOT
  template_author: visionspace

description: File types for any systems.

imports:
  - tosca-normative-types:1.0.0-ALIEN14

node_types:
  org.alien4cloud.nodes.File:
    derived_from: tosca.nodes.Root
   
    metadata:
      icon: /images/javascript.png
    properties:
      port:
        type: integer
        default: 3306
        description: The port on which the underlying database service will listen to data.
      use_sudo:
        type: boolean
        default: true
        description: >-
          Whether or not to use sudo to move, rename, delete, chown,
          chmod,           the file.
        required: true
      permission:
        type: integer
        default: ''
        description: >-
          The file permissions, such as 777. Must be provided as an
          integer.           "0777" and 0777 are not valid. Only 777.
        required: false
      owner:
        type: string
        default: ''
        description: 'The owner string, such as "centos:wheel"'
        required: true
       
      file_path:
        type: string
        default: ''
        description: File_Path
        required: false
        
    

  

